apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "automl-helm.fullname" . }}-test-connection"
  labels:
    {{- include "automl-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "automl-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

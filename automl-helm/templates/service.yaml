apiVersion: v1
kind: Service
metadata:
  name: {{ include "automl-helm.fullname" . }}
  labels:
    {{- include "automl-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "automl-helm.selectorLabels" . | nindent 4 }}
